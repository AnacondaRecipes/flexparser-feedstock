anaconda_config_version: 1
upstream_sources:
  - pypi:
      identifier: flexparser
      strip_prefix: v
    packages: [flexparser]
  - github:
      identifier: hgrecco/flexparser
      strip_prefix: v
    packages: [flexparser]
